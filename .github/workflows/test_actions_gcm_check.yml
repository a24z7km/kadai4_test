name: Test GCD Script

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: テストスクリプトを実行
      run: ../test_cal_gcm_check
